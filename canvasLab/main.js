require.config({
    baseUrl:"../vendor",
    shim: {
        'jquery-eraser': ['jquery']
    },
    paths: {
    "jquery": "jquery-2.1.4",
    "jquery-eraser":"fingerEraser",
    "underscore": "underscore.min",
    "backbone": "backbone.min",
     "tomLib":"tomLib",
     "zepto":"zepto.min"
    }
});

